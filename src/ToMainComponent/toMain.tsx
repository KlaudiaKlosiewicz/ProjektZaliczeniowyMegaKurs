import React from "react";

const toMain = (props: any) => {
    return (

        // <button onClick={props.buttonEventListener}>Powrót na stronę główną</button>
        <a href="/" title="" className="backBtn" role="button">Przejdź na stronę powitalną</a>

    )
}

export default toMain;